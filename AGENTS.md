# PV 制作规范

本文件是 `pv` 目录内宣传视频制作、预览和代码修改的长期约定。后续修改 PV 时必须遵守以下规则：

## 修改后的验证

- 修改代码完成后，只进行语法级验证（例如 TypeScript / JSX 类型或编译语法检查）。
- 除非用户明确要求，否则不进行截图验证、浏览器视觉验证、完整渲染或其他耗时的结果验证。
- 发现语法错误时修复后重新进行语法级验证；不要擅自扩大验证范围。

## 渲染与导出

- 修改完成后不自动导出 MP4，也不生成完整视频文件。
- 默认让用户通过 Remotion Studio 网页实时预览修改结果。
- 只有在用户明确表示 PV 全部制作完成并要求导出时，才执行最终视频导出。
- 生成 Live2D 或其他中间素材也应以完成当前必要工作为限，避免无请求的长时间渲染。

## 修改范围

- PV 相关修改只放在 `pv` 目录内，不修改 Electron 主项目代码、后端 API、Live2D 原始模型或 Q 版静态吉祥物。
